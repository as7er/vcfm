# 颜色去重（2026-09-16）

> 纯表现层：`css/style.css` 一处文件、**82 处**改动。没有改任何机制、数据或模拟。
>
> ⚠ **本轮的诚实边界**：只做了**可证明是「无操作」**的那一类去重（82 处）。
> 剩下 **313 处基础作用域**的字面值**有意没动** —— 因为项目的**每一个令牌都随主题
> 变化**，把基础作用域的字面值换成令牌会**悄悄让它变成随主题变化**，那是真实改变，
> 不是去重。理由与后续方案见 §4。

## 1. 改前的量

| 指标 | 数值 |
|---|---:|
| 硬编码 hex 出现次数 | **500**（`style.css` 480 / `main.js` 18 / `index.html` 2） |
| 不同 hex 值 | **165** |
| `var(--token)` 引用 | 572 |
| `rgba()` 字面值 | 589 |

**两套并行调色板**（同一份文件里）：

| 项目令牌 | 并行的 Tailwind 风格字面值 |
|---|---|
| `--muted: #8b9bb8` | `#94a3b8`（slate-400，13 次） |
| `--text: #e8eefc` | `#e2e8f0`（slate-200，15 次） |
| `--bad: #f07178` | `#f87171`（red-400，10 次） |
| `--warn: #e6b450` | `#fbbf24`（amber-400，14 次） |
| — | `#93c5fd` 12 次、`#86efac` 12 次、`#fca5a5` 9 次… |

外加**同一颜色两种写法**：`#fff` 41 次 + `#ffffff` 17 次。

## 2. 为什么颜色比字号难：每个令牌都是主题相关的

字号只有一套值，所以「字面值 → 令牌」总是保值的。颜色不是：

```
--primary  深色 #3d8bfd   浅色 #2563eb
--warn     深色 #e6b450   浅色 #a16207
--border   深色 #2a3650   浅色 #c5d0e3
```

**26 个颜色令牌全部随主题变化**。所以一条写在基础作用域（两主题都生效）里的
`color: #3d8bfd`，换成 `var(--primary)` 之后在浅色下会变成 `#2563eb` ——
这是**真实改变**，不是去重。

架构上这是有意为之：基础规则按深色写，浅色由
`/* ===== 日间模式全面补丁 ===== */`（`css/style.css:7252`）那一节逐条覆盖。
`css:2918` 有一条注释正好说明了这个模式：

> 浅色主题：上面整块是按深色面板配的（半透明深底 + 近白正文 + 亮黄强调）。
> 叠到白底上底色变浅，#e2e8f0 正文和 #fde047 黄标题就成了白底白字 / 白底黄字。

## 3. 本轮做了什么：82 处可证明的无操作

审计工具 `scripts/_color-dedup-audit.mjs` 按「是否已在主题作用域内」给每处 hex 分类：

| 位置 | 出现次数 | 说明 |
|---|---:|---|
| 基础作用域 | 297 | 两主题都生效 → 换令牌会变语义 |
| **主题作用域内** | **183** | 已限定主题 → 换令牌可保值 |

再从 183 里筛出「值恰好等于**该主题**的令牌值」的：**65 处**。
加上 **17 处纯拼写**（`#ffffff` → `#fff`），共 **82 处**。

**关键的一步自我纠正**：脚本第一版在「一个值对应多个令牌」时**取了第一个**
（`#1e293b` 同时是 `--toast-bg` / `--surface-inset` / `--bar-track`；
`#c5d0e3` 同时是 `--border` / `--board-border` / `--input-border`）。
那不是去重，是**臆造语义**。改成**只处理唯一对应的值**后，从 98 处降到 65 处。

**改动的效果**：

| 指标 | 改前 | 改后 |
|---|---:|---:|
| `style.css` hex 出现 | 480 | **415** |
| 全仓不同 hex 值 | 165 | **141** |
| `#ffffff` 残留 | 17 | **0** |

## 4. 没动的是什么，以及为什么

**基础作用域 313 处**（110 个不同值）**全部保留**。其中：

| 分类 | 不同值 | 次数 | 占比 |
|---|---:|---:|---:|
| 出现 ≥3 次（复现调色板） | 32 | **211** | 67% |
| 出现 1–2 次（一次性装饰色） | 78 | 102 | 33% |

复现的那 32 个构成六族并行调色板：

```
中性   #fff(24) #e2e8f0(15) #f8fafc(11) #94a3b8(11) #cbd5e1(11) #64748b(5) #0f172a(3) #111827(3)
琥珀   #fbbf24(13) #f59e0b(6) #fde68a(4) #f97316(4) #fb923c(4) #fcd34d(3) #fdba74(3)
绿     #86efac(7) #166534(5) #22c55e(5) #4ade80(4)
红     #f87171(8) #fca5a5(6) #ef4444(5) #b91c1c(4)
蓝     #93c5fd(10) #3d8bfd(7) #1d4ed8(5) #38bdf8(5) #60a5fa(4)
紫     #a78bfa(6) #c4b5fd(4)
```

主题作用域内还剩 45 个不同值 / 69 次「不等于任何令牌」，其中大部分是
**一次性渐变端点**（`#b8d4ff` / `#7ecf9a` / `#8eb8f0` / `#6aaf7a`… 出现在
开始页的球场渐变里），本就不该令牌化。

### 后续方案（**需要视觉复核，不要盲改**）

把基础规则改成**主题相关**、然后**删掉对应的浅色补丁**。这是真正的重构方向，
但它要求：

1. 给六族调色板各自定义**按角色的令牌**（如 `--danger-soft` / `--danger` / `--danger-strong`）；
2. 逐条把基础规则里的字面值换成令牌；
3. 逐条删掉 `日间模式全面补丁` 里因此变成多余的规则；
4. **两个主题、逐屏截图比对** —— 这一步无法自动化，`browser-e2e` 只验行为不验观感。

工作量与风险都明显高于本轮：它一次改动 300+ 处，且每一处都需要判断
「这个字面值在浅色下应该是什么」。所以本轮**只做能证明是恒等变换的那 82 处**。

## 5. 验收

### 5.1 静态证明（本轮的主要证据）

`scripts/_verify-color-dedup.mjs` 用 `git show HEAD:css/style.css` 取改动前的版本，
把每一行变化里的「颜色项」按顺序配对（`var(--token)` 与 `#hex` 都算一项），
**在该行所属的主题作用域里解析令牌值**，再与原来的字面值比较（`#fff` 与 `#ffffff`
先规范成 6 位再比）：

```
{"rev":"HEAD","changedLines":81,"replacementsChecked":85,"mismatches":0,"unpairedLines":0}
PASS: every replacement is value-preserving
```

**85 项逐一比对、0 处不一致** —— 即本次改动在渲染上是**恒等变换**。

> ⚠ 验证器第一版报 `changedLines: 8331`（全文都"变了"）。原因是
> **git 存的是 LF，工作区是 CRLF**，逐行比较时每行都因行尾不同而判为变化，
> 配对随即失去意义。归一化行尾后才得到 81 行。
> **在 Windows 上做逐行对比，先归一化行尾。**

### 5.2 浏览器验收

`browser-e2e.mjs` 与双主题 `_ui-survey.mjs`（本轮给调研探针新增了 `[theme]` 参数，
`light` / `dark` / `both`）。见 §6 日志。

⚠ **本轮的浏览器验收是「防语法错误」而非「验保值」**：保值已由 §5.1 静态证明。
浏览器跑一遍是为了确认没有写坏 `var()`、没有 CSS 解析错误。
`#fff` → `var(--card)` 那 16 处**曾经**是我第一版的错误产物（按钮背景被当成卡片
背景），已在 §3 的唯一性约束下全部排除。

### 5.3 缓存

`sw.js` 的 `CACHE`、`index.html` 的 `CURRENT_CACHE`、三处 `?v=`、
`vcfm-sw-reloaded-v` 同步升 **v260 → v261**。

## 6. 复现

```bash
# 审计：按主题作用域分类，列出可证明无操作的别名
node scripts/_color-dedup-audit.mjs
node scripts/_color-dedup-audit.mjs --json

# 去重（先干跑看明细，再 --write）
node scripts/_color-dedup.mjs
node scripts/_color-dedup.mjs --write

# 静态证明：每一处替换都保值
node scripts/_verify-color-dedup.mjs

# 双主题逐页普查（12 桌面页 + 5 手机页 × 2 主题）
node scripts/_ui-survey.mjs .tmp-continuity/ui-survey-color both

node scripts/browser-e2e.mjs
```
