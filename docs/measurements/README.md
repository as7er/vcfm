# 探针原始读数归档

本目录保存**历史引擎状态**上的探针原始输出，用于 A/B 对账。
归档理由：`git worktree` 目录下的临时文件在移除 worktree 时会丢失，
而「同一探针、同一批种子、不同引擎」的原始输出是结论的**唯一凭据**。

## 文件

| 文件 | 引擎 | 提交 | 场数 | 种子 | 口径 |
|---|---|---|---:|---|---|
| `probe-final-third-48-v263-3f10fd5.txt` | v263 | `3f10fd5` | 48 | 372000..372047 | 能力 15、同强度、标准档、0.1s |
| `probe-final-third-48-v252-468816e.txt` | v252 | `468816e` | 48 | 372000..372047 | 同上 |
| `probe-final-third-12-v251-524589d.txt` | v251 | `524589d` | 12 | 372000..372011 | 同上 |
| `probe-final-third-12-runC-levels-3f10fd5.txt` | v263 | `3f10fd5` | 12 | 372000..372011 | 同上；**跑动原语首筛**（runC1.0/2.0/3.0，见下） |
| `probe-final-third-12-runMargin-levels-213991b.txt` | v263 | `213991b` | 12 | 372000..372011 | 同上；**runMargin 配平曲线**（control/margin2/4/6） |
| `probe-final-third-48-runMargin-margin2-213991b.txt` | v263 | `213991b` | 48 | 372000..372047 | 同上；**runMargin 配平点同种子 A/B** |
| `probe-final-third-6-runMargin-margin2-213991b.txt` | v263 | `213991b` | 6 | 372000..372005 | 同上；**与 `box-possession-sampling-audit` 同窗口** |
| `probe-final-third-12-holdPass-93fb16c.txt` | v263 | `93fb16c` | 12 | 372000..372011 | 同上；**`holdPass{4,6,8,10}` 触发窗口曲线**（⛔ 等量交换，见下） |
| `probe-final-third-6-runmin-1b958f3.txt` | v263 | `1b958f3` | 6 | 372000..372005 | 同上；**`runMin{2,4,7,10}` + `runLead{1.5,2.5}`**（⛔ 无效/同构，见下） |
| `probe-final-third-6-runmin-isolated-1b958f3.txt` | v263 | `1b958f3` | 6 | 372000..372005 | 同上；**隔离复跑**，用来定位 `sweep()` 清理粘性缺陷 |
| `probe-box-seconds-rebaseline-6-2026-09-18.txt` | v263 | `a53cda8` | 6 | 372000..372005 | **审计口径桥接**：control 逐位对齐审计 679.53 |
| `probe-box-seconds-rebaseline-24-2026-09-18.txt` | v263 | `a53cda8` | 24 | 372000..372023 | 同上 |
| `probe-box-seconds-rebaseline-24b-2026-09-18.txt` | v263 | `a53cda8` | 24 | 480000..480023 | 同上；**换窗口复核** |
| `probe-boxsec-noise-control-2026-09-18.txt` | v263 | `a53cda8`+探针 | 48 | 372000..372047 | **boxSec 噪声标定**：8 批 × 6 场，control |
| `probe-boxsec-noise-margin2-2026-09-18.txt` | v263 | `a53cda8`+探针 | 48 | 372000..372047 | 同上；`margin2` |
| `audit-box-possession-6-baseline-2026-09-18.txt` | v263 | `a53cda8` | 6 | 372000..372005 | 审计原始读数（桥接自检的对照源） |
| `audit-box-possession-24-baseline-2026-09-18.txt` | v263 | `a53cda8` | 24 | 372000..372023 | 同上 |
| `ht-sheet-broken-phone-390x844.json` | — | `3f10fd5`+探针 | — | — | 中场抽屉**修复前**布局读数（两面板 16px） |
| `ht-sheet-fixed-phone-390x844.json` | — | 修复后 | — | — | 中场抽屉**修复后**布局读数（182/222px） |

## 跑动原语首筛（2026-09-17，12 场粗筛）

`probe-final-third-12-runC-levels-3f10fd5.txt` 的关键读数（探针档位，引擎零改动）：

| 档位 | 领先≥5m | 领先中位 | 近静止 | 均速 | 越位 | 进球 |
|---|---:|---:|---:|---:|---:|---:|
| control | 8.5% | −10.6m | 43.6% | 1.83 | 1.75 | 2.25 |
| runC1.0 | **11.1%** | **−7.38m** | 38.4% | 2.01 | 2.67 ⚠ | 2.33 |
| runC2.0 | 10.3% | −7.85m | 40.8% | 1.94 | 3.42 ⚠ | 2.58 |
| runC3.0 | 10.2% | −8.26m | 43.7% | 1.87 | 2.67 ⚠ | 2.42 |

⇒ 纵深前进 2.3~3.2m（七次尝试首次），但越位 2.67~3.42 超真实带 1.4~2.1。
已加 `runMargin` 档位（线后 2/4/6 单位），**已于 2026-09-18 上午跑完**——见下节。

## runMargin 配平（2026-09-18，引擎零改动）

`probe-final-third-12-runMargin-levels-213991b.txt`（12 场，种子 372000..372011）：

| 档位 | 越位 | 进球 | 射门 | 传中 | 领先≥5m | 领先中位 |
|---|---:|---:|---:|---:|---:|---:|
| control | 1.75 ✅ | 2.25 | 12.00 | 3.36% | 8.5% | −10.60m |
| **runC1.0+margin2** | **1.88** ✅ | 2.42 | 12.96 | 4.04% | **10.1%** | **−7.38m** |
| runC1.0+margin4 | 1.25 | 2.58 | 11.58 ⚠ | 3.53% | 8.0% | −7.85m |
| runC1.0+margin6 | 1.38 | 2.42 | 12.08 | 3.14% | 8.0% | −8.16m |

⇒ **配平点在 margin2**：只有它同时满足「越位进带」+「纵深保留」。
margin4/6 是**双向反向**——越位被压到带外下沿（1.25/1.38），而纵深改善同时衰减。

`probe-final-third-48-runMargin-margin2-213991b.txt`（48 场同种子 A/B，`[0]` 自检 ✅）：

| 指标 | control | margin2 | 判定 |
|---|---:|---:|---|
| 领先中位 | −11.28m | **−7.33m** | ✅ |
| 领先≥5m | 8.3% | **10.3%** | ✅ |
| 越位/队场 | 1.65 | **1.93** | ✅（余量仅 0.17） |
| **直塞/场** | 2.02 | **1.88** | ~~❌ 判据要求 ↑（反向）~~ **⛔ 该判决已作废，见下节** |
| **boxSec/场** | 679.05 | **818.18** | ❌ 判据要求不显著回升（+20.5%） |

⇒ **不落引擎**。归因：**boxSec 回升来自 `runCommit` 原语本身，不是 `runMargin`**
（12 场里不带 margin 的 `runC1.0` 已是 826.58，margin2 是 832.7，差 0.7%）。
⚠ `probe-final-third-6-runMargin-margin2-213991b.txt` 是**与 `box-possession-sampling-audit`
同窗口**（种子 372000..372005）的复核：margin2 的 boxSec 在该窗口是 **855.58，顶破审计 850 天花板**。

⚠ **跨口径提醒**：本目录的探针 `club()` 带 `crossing` 属性，而 `box-possession-sampling-audit.mjs`
的 `makeClub()` 不带（17 vs 18 项）；同种子 6 场受控 A/B 下 boxSeconds 差 **+51.5s**。
⇒ **探针与审计测的不是同一支球队**，跨口径比 boxSec 时要把这点算进去
（详见 [docs/handoff-runmargin-2026-09-18.md](../handoff-runmargin-2026-09-18.md) §4）。

## boxSec 取舍：`holdPass` + 路 3 全部走完，**没有中间点**（2026-09-18 下午，引擎零改动）

`probe-final-third-12-holdPass-93fb16c.txt`（12 场）与
`probe-final-third-6-runmin-1b958f3.txt` + `probe-final-third-6-runmin-isolated-1b958f3.txt`（6 场）。

| 档位 | boxSec | 领先≥5m | 领先中位 | 越位 | 承诺覆写 |
|---|---:|---:|---:|---:|---:|
| `control`（12 场） | 703.36 | 8.5% | −10.60m | 1.75 | — |
| `runC1.0+margin2`（常驻） | **832.70** | **10.1%** | **−7.38m** | 1.88 | 135716 |
| `holdPass6` | **625.88** | 7.7% | −11.15m | 1.88 | 9827 |
| `runMin2/4/7/10`（6 场） | **855.58（四档逐字相同）** | 10.0% | −7.41m | 2.25 | **67638（逐字相同）** |
| `runLead1.5`（6 场） | 815.70 | 10.4% | −7.55m | **1.08（跌出真实带）** | 67145 |
| `runLead2.5`（6 场） | 735.35 | 9.7% | −8.50m | 1.92 | 66275 |

**三条结论：**
1. **`holdPass` = 等量交换**：boxSec 解决了（625.88，比 control 还低 11%），**纵深改善同时消失**。
   根因是**时序错配**：球刚赢回时还在中前场，等推进到最后三区窗口早关。扩窗无效（4s→10s
   承诺升 2.6 倍，纵深不动）。
2. **`runMinDist` 完全无效**：四档 boxSec / 承诺覆写 / 领先中位**逐字相同**。
   门槛只改「谁进入承诺」，改不动「球员跑多远」——承诺的动作是「把 `a.ty` 掰回快照」。
3. **`runLead` 与 `runMargin` 同构**：`runLead↑ ⇒ boxSec↓ 且纵深↓`，单调。
   `runLead1.5 / margin2 / runLead2.5` 三点落在同一条线上 —— **不是独立旋钮。**

⇒ **原语的验收指标本身在打架**：它的效果是「让球员更靠近对方球门」，
「目标领先球」读数改善、「boxSeconds」读数回升，**同一件事两个方向**。
剩两条真路：**(1) 接受 `margin2` + 重标 850 天花板**；**(2) 窗口改成「球进最后三区后才开窗」**（未测）。
详见 [docs/through-pass-gate-and-player-ability-2026-09-18.md](../through-pass-gate-and-player-ability-2026-09-18.md) §3.2 / §3.3。

⚠ **仪器缺陷（本轮暴露，未修）**：`sweep()` 的收工清理**漏复位**
`runCommit/runLead/runMargin/runWindow/runMinDist` ⇒ **纯 `control` 档会继承上一档的跑动原语**。
**临时纪律：纯 control 档必须排在所有带 `runCommit` 的档之前**（档位表已满足）。
证据：一次性跑全表时 control 读到 855.58，单独复跑时读到 726.52。
⚠ 该缺陷**不影响 `holdPass` 那轮数据**（那轮 control=703.36 / runC1.0=808.72 / holdPass6=625.88 互不相同）。

## boxSec 跨口径问题已解决：`_box-seconds-rebaseline-probe.mjs`（2026-09-18 晚）

交接文档长期挂着一条**未解决的口径不一致**：跑动原语的 boxSec 只在
`_final-third-movement-calibration-probe.mjs` 里测过（`club()` 是 **18 项属性、带 `crossing`**），
而 `box-possession-sampling-audit.mjs` 的 `makeClub()` 是 **17 项属性、无 `crossing`**
（同种子 6 场受控 A/B 差 **+51.5s**）。

**新探针 `scripts/_box-seconds-rebaseline-probe.mjs` 把它修好了**：逐字搬用审计的球队构造 /
回合判据 / 采样循环，只多挂一个跑动原语。**自检 = `control` 的 boxSeconds 必须与审计逐位相同**：

| 窗口 | 探针 control | 审计 | 判定 |
|---|---:|---:|---|
| 6 场 372000..372005 | **679.53** | **679.53** | ✅ |
| 24 场 372000..372023 | **693.30** | **693.30** | ✅ |

⚠ 保留：探针 `boxSpells` 比审计低（43.6 vs 216.8；审计按「换持球人」开回合，探针按「换持球方」）。
`boxSeconds` 逐位相同，故对护栏判定有效；**别用 boxSpells 跨工具比**。

### 复核推翻了「margin2 破 850」

| 窗口 | control | margin2 | 判定 |
|---|---:|---:|---|
| 6 场 | 679.53 | **878.28** | ❌ 破 |
| 24 场 372000.. | 693.30 | **830.98** | ✅ 过 |
| 24 场 480000.. | 666.75 | **864.01** | ❌ 破 |

**`margin2` 的 24 场均值 847.50 vs 护栏 850 ⇒ 只低 2.5；窗间 SD = 23.36。**
⇒ **护栏落在噪声尺度内，24 场分不出过/破。** 6 场的 878.28 是小样本偏移
（同时刻 `control` 也偏高）。**因此不落引擎、也不重标。**

### 噪声标定做完了（48 场 × 2 档，8 批 × 6 场）—— 护栏确认为**不可判**

同型方法（§ 直塞那节）：**同一引擎、同一档位，跑 8 批互不重叠的 6 场窗口（种子 372000..372047）**。
归档：`probe-boxsec-noise-{control,margin2}-2026-09-18.txt`。

| | control | `margin2` |
|---|---:|---:|
| 48 场总体均值 | **678.03** | **838.12** |
| 批间标准差（每批 6 场） | **59.01** | **47.89** |
| 批内均值极差 | 565.82 ~ 738.87（173.05） | 754.37 ~ 900.68（146.32） |
| 逐场标准差 | **148.20** | **100.95** |
| 48 场读数标准误 | 21.39 | **14.57** |
| 单场最窄 / 最宽 | 326.5 / 990.9 | 630.4 / 1124.7 |

**两点必须分开读：**

**① 护栏不可判（确定）。** `margin2` 48 场均值 838.12 vs 护栏 850，差 **11.88**；
而 48 场读数 **2SE = 29.14** ⇒ 11.88 < 29.14，**加样本也判不了**。
逐场 SD 高达 **148s**（单场 326~991）⇒ 检出 20s/场 需要每组约 **440 场**，10s/场需 **1758 场**。
⇒ **850 在 ≤48 场规模下没有判决力**，性质等同「别整体消失」（与直塞 `>= 0.5 && <= 12` 同型）。

**② 原语效应本身极大（同样确定）。** 效应 **+160.09s**（838.12 − 678.03），
48 场两独立样本 2SE 门槛仅 **58.28** ⇒ 效应是门槛的 **2.75 倍**。
**「跑动原语让 boxSec 涨约 160s」48 场已判死**；6 场读出的 +199s 方向对、**幅度被夸大**。

⇒ **处置**：① `boxSec <= 850` 从「验收判据」降级为「爆炸半径限制」；
② **原语不落引擎** —— 不是「破护栏」（破没破判不了），而是 **+160s 是已知的实质代价，
而纵深改善尚未证明能传导到任何终局指标**；③ 若要继续，前置是先给**进球**补护栏
（与直塞、角球同型的仪器缺口），证明纵深能传导，再谈落引擎。

详见 [docs/through-pass-gate-and-player-ability-2026-09-18.md](../through-pass-gate-and-player-ability-2026-09-18.md) §3.4。

## 直塞噪声标定：那条「直塞 ❌ 反向」的判决**已作废**（2026-09-18）

新探针 `scripts/_through-pass-noise-calibration-probe.mjs`（纯测量，不改引擎）：
**同一引擎、同一档位，跑 20 批互不重叠的 12 场种子窗口（共 240 场）**，
直接测「换一批种子，读数会漂多少」——这正是上面那张表里缺的那个 SE。

| | control（引擎原样） | runC1.0（挂跑动原语） |
|---|---:|---:|
| 240 场总体均值 | **2.33** | **2.20** |
| 批间标准差（每批 12 场） | **0.38** | **0.49** |
| 批内均值极差 | **1.67 ~ 3.08**（1.42） | **0.83 ~ 3.00**（2.17） |
| 逐场标准差 | 1.49 | 1.46 |
| 0 次的场数占比 | 8.75% | 9.58% |

原始输出：`probe-through-noise-240-control-8b43f92.txt` / `...-commit-8b43f92.txt`。

### 为什么判决作废

被判「反向 ❌」的效应是 **0.14/场**（2.02 → 1.88）。而：

- **48 场 A/B 的 2SE 门槛 = 0.61** ⇒ 0.14 只有门槛的 **23%**，**根本不可能被判有差**；
- 检出 0.30/场 的效应需 **每组约 295 场**；检出 0.14/场 需 **每组约 1352 场**；
- **同一档位在 6 场是 2.17 → 2.67（差 +0.50，方向相反）**——
  同一实验换个窗口就翻向，是噪声主导的直接证据。

⇒ **直塞不可用作「近 48 场判 ↑/↓」的细粒度判据。**
它只能用于**大样本方向性参考（≥200 场/组）**。

### 连带结论：现状那条「护栏」的真实性质

`match-realism-audit.mjs:463` 的 `throughPasses >= 0.5 && <= 12`：

- **上下沿差 24 倍**，落在区间中央毫无信息量；
- 实测均值 2.2~2.33，**贴在旧断言下沿 0.5 上方 4.4 倍**，从来不是「标定区间」；
- ⇒ 它的真实功能是**爆炸半径限制**（防这项指标整体消失/爆表），
  **不是**「落在标定区间内」的质量判据。文档表述应照此改述。

⚠ 与「进球只有一种口径」「角球只有单侧下限」**同型的仪器问题**：
凡「护栏区间极宽 + 指标方差大 + 被当作方向判据用」三者同时成立的量，都要先做一次
像本轮的噪声标定，再决定它能不能进验收表。

## 中场抽屉布局修复（2026-09-17）

`ht-sheet-*.json` 是同一视口（phone 390×844）修复前/后的盒模型读数：

| 面板 | 修复前可视高 | 修复后可视高 | 内容高 |
|---|---:|---:|---:|
| `#match-ht-fitness` | **16px** | **182px** | 270px |
| `#match-ht-roles` | **16px** | **222px** | 1140px（phone 窄屏 1 列） |

复现/验证命令：
```
node scripts/_halftime-sheet-layout-probe.mjs <outDir> --shots        # 实验室（5 候选 × 5 视口）
node scripts/_halftime-sheet-layout-probe.mjs <outDir> --verify --quick --shots  # 只测真实 CSS + phone
```

复现命令（在对应版本的 `git worktree` 内）：

```
node scripts/_final-third-movement-calibration-probe.mjs 48 "control"
```

## 已丢失的原始文件

`probe-final-third-12-v251-524589d.txt` 的**原始输出**在移除
`../VCFM-v251` worktree 时随目录丢失。下表的关键行转抄自当时的终端输出，
**属于二手记录**；若要再引用，请重新 `git worktree add` + 重跑（约 6 分钟）。

v251 `524589d` @ 12 场 / 种子 372000..372011 / control：

```
越位 3.13  进球 2.50  射门 14.08  传球 1133.75  传中 4.52%
禁区触球 300.5  boxSec 1119.17  直塞 1.42
近静止 44.2%  均速 1.97 m/s  目标距离 4.44 m  最后三区 3000.65s  boxSec占比 37.3%
已到位 42.7%  该跑没跑 1.5%
领先≥5m 9.7%  领先中位 -16.23m  越过线 2.7%  距防线中位 19.49m
```

## 读这些文件的纪律

1. **12 场不能看进球。** 12 场进球标准误约 ±0.35；同一批引擎的四组 12 场读数
   分别是 2.50 / 2.50 / 3.50 / 2.25（抖动 1.25 球）。
2. **跨版本不能相减**，除非两边都是**实跑**出来的（见上表「提交」列）。
   文档表格里没有「引擎提交」列的数字，只能当方向性预期。
3. 每份输出开头的 `[0] 包装自检` 必须显示「✅ 一致」——
   否则 control 档有侧效应，全表作废。
