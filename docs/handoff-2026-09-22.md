# 换机交接（2026-09-22 深夜）

> 交接时状态：**工作区干净，全部改动已提交并推送**。
> 缓存 **vcfm-v281**。无 worktree、无后台进程、无待接管任务。
>
> **代码 HEAD = `cdbd1c1`**（v281）。
> ⚠ **本文档自身是在它之后提交的** —— 所以你 pull 下来时
> `git rev-parse HEAD` 会是 `c01a8bd` 或更新，**这是正常的**，别当成出错。
> 想确认「代码那一版」在不在，看 `git log` 里能否找到 `cdbd1c1` 即可：
>
> ```bash
> git switch master
> git pull --ff-only
> git log --oneline -1                    # 应为 c01a8bd 或更新（本文档那次提交）
> git cat-file -e cdbd1c1 && echo "代码 HEAD 在"
> git ls-remote origin refs/heads/master  # 应与本地 HEAD 一致
> ```
>
> ⚠ 本仓库历史上出现过 `git fetch` **静默不写远端跟踪引用**的情况 ⇒ **一律以 `ls-remote` 为准**。
>
> 续接先读：**本文档** → `AGENTS.md` 页首（v277~v281 五段）→
> `docs/measurements/tactics-layer-four-items-2026-09-22.txt`（本会话最全的一份，末尾有 ④-B 配方）。
> 不必先跑 `verify.mjs`（114 项，全量约 43~57 分钟）。

## 1. 一句话留给下一个接手者

本会话从「用户刷新后仍觉得直播问题多」开始，**修掉了三类真缺陷**（切段淡场 3 处、
终场体能掷骰子、**换边方向/端别一整类 49 行**），并**查清两件用户报的事**：
「重开画面一样」**不是 bug 是设计**（已加提示）、「球员读懂战术」**大体读得懂但有 4 个缺口**
（改了 1 处、撤回 1 处、2 处留作待拍板）。

**下一步最该做的是 ④-B「真正的继续看」—— 配方已查实（归档附录），约 15 行，不必改 `js/match.js`。**

## 2. 当前 HEAD 链（本会话，全部已推送）

| commit | 内容 | 动引擎？ |
|---|---|---|
| **`cdbd1c1`** | **v281** 重开直播提示（进度记录 + 双语弹窗 + 替代动作） | 否 |
| `32709e5` | **v280** 战术层：`pressing` 联动防线 + 两条查证 + 快档回归常驻 | 是（`_defLineY`） |
| `b2d38df` | 战术层/个体层摸底（只读探针 + 归档） | 否 |
| `7216ad5` | **v279** **换边（下半场）方向/端别大修** | **是（大改）** |
| `aa6d100` | **v278** 终场体能改为按跑动距离分摊 | 是（`drainFitness`） |
| `721d807` | **v277** 切段淡场修三处 | 否（表现层） |
| `5bb3a7c` | A 线判决（瞬移 = 场景切换；开球多闪一次的守卫失效） | 否 |

## 3. 本会话修了什么（按「用户能看到的」排）

| # | 用户看到的现象 | 根因 | commit |
|---|---|---|---|
| 1 | **下半场进球记到对方头上**，还显示「B 队 X 乌龙」，**记分板/联赛积分都错** | `_resolveBounds`（`engine.js:7525`）把「主队球门在 y≈100」写死了。实测 46→90 分钟 **8 球 8 反** | v279 |
| 2 | 门将站在**球网里面** | `clampGkY`（`engine.js:2336`）按队名取「门内侧」，而 `goalY` 已随换边翻转。实测 `ty = −1.00 / +101.00` | v279 |
| 3 | **越位基本不吹**；越位线画在反方向 | `_isOffsidePosition` 把「在对方半场」写死 `y<50`；`_offsideLineY`（`engine.js:5494`）用队名判「谁更靠前」 | v279 |
| 4 | **你报的：下半场角球/门球判错、摆错端** | `_resolveBounds` 里 4 处 `_restart` 的 y 写死（4/12/96/88） | v279 |
| 5 | **前场任意球整队跑到另一端** | `_restart` 的 `mirrorY` 按队名镜像。实测 10 人里 8 人被摆到 y≈14-28 | v279 |
| 6 | 进球瞬间**球瞬移 ~87 码** | `_goal` 钉球端别与记名**同向错误、互相抵消**（必须同批改） | v279 |
| 7 | **切段时整队瞬移仍可见 28%** | 淡场峰值只到 0.72；reduced-motion 下更是完全透明 | v277 |
| 8 | **每场开球多闪一次淡场** | `Number(null)===0` 穿过 `isFinite` 守卫 | v277 |
| 9 | 体能面板「各队员显示不对」的另一半 | 终场那笔 4~9 点（均值 6.5）是**逐人掷骰子**，与跑动无关（相关性 −0.007）—— `js/match.js:3109 drainFitness` | v278 |
| 10 | 「压迫拉满但防线不动」 | `pressing` 完全不进防线计算（`_defLineY`） | v280 |
| 11 | 「重开直播画面一样」 | **不是 bug 是设计**（见 §4.1） | v281 |

## 4. 查清但**没改**的两件事（都是用户报的）

### 4.1 「重开直播画面一样」= 设计（v281 加了提示）

- `js/main.js:10201 openMatch()` **无条件重置**（`matchState=null` / 分钟归 0 / 清播放态）；
  `runMatch` 新建会话；`js/match.js:2202 playFirstHalf` 的 `fromMin` **硬编码 1** ⇒ 从 0′ 重播；
- **全仓没有任何「续播」状态**（`fromMin` 只有 1/46/61/76，无一处读 `state.minute`）；
- `matchSeed` 随存档保留 ⇒ 随机流相同 ⇒ **重放逐位相同**；且**赛中不落盘**
  ⇒ F5 读到的是开赛前的 world ⇒ 重放**逐位相同**。

**v281 只做了「提示 + 替代动作」**：进度写 `sessionStorage`（定义 `js/main.js:515 writeMatchProgress`，
钩在 `js/main.js:10372 setMatchMinute` 的 `:10386`），`openMatch` 末尾调
`js/main.js:10290 maybeWarnReopenedMatch` 弹双语提示
（「从头重看」只关弹窗 /「直接出战报」→ `runMatch("instant")`）。
**弹窗里没有任何续播按钮**（避免假承诺）。

⚠ **仍需用户确认**：比赛屏**没有返回按钮**，所以"退出比赛界面"只能是刷新/关标签页；
若用户用的是浏览器**后退**（bfcache 恢复），JS 状态还在，**画面上什么都不会重播** —— 情况不同。
判据：控制台看 `performance.getEntriesByType("navigation")[0].type` 是 `reload` 还是 `back_forward`。

### 4.2 「球员读懂战术」摸底：**大体读得懂，4 个缺口**

| 旋钮 | 实测 | 判定 |
|---|---|---|
| `width` | 横向 **+44%**（12.7→18.3 m，10~20σ） | ✅ 读懂 |
| `defensiveLine` | DEF 无球纵深 **+11.4 格（≈12 m）**，且 **ATT 基本不动** | ✅ 读懂 |
| `tempo` | 持球时长 **−15%**（3.60→3.05 s，6σ） | ✅ 读懂 |
| `style` | attack 比 defend 高 4.4 格（代码预期 5.5） | ✅ 吻合 |
| `pressing` | **改前完全不推防线** ⇒ **已修**（v280，改后 18.9/21.6/23.1 单调 +4.2 格） | ✅ 已修 |

**4 个缺口 = §5 的「第二 / 第三优先」。**

## 5. 下一步：按优先级 + **完成判据**

### ⭐ 第一优先：④-B 真正的「继续看」（配方已查实，约 15 行）

**完整配方在归档末尾**（`docs/measurements/tactics-layer-four-items-2026-09-22.txt` 的「附」一节）。
核心依据两条：

1. `js/match.js:1663-1694`：`playHighlightPlan` 返回后有**兜底循环**
   （flush 所有剩余线索 `:1685-1693` + 对每个分钟跑 `finishMinuteSideEffects` `:1694`）
   ⇒ **渲染与副作用分离**，一个段都不渲染也不会丢进球/体能/事件。
2. `js/main.js:9664 playHighlightPlanBridge` 的段循环（`:9699`）**已有**「不渲染、只记账」的分支
   （`:9700-9706`：`spec.onSkip?.(seg)` + `refreshLiveHudFromState(seg.toMin, seg.t1)`）。

**做法**：加 `main.js` 模块级 `matchResumeSimT`，在段循环（`:9699`）**开头**插一句 ——
`seg.t1 <= matchResumeSimT` 时走 `spec.onSkip(seg)` + `refreshLiveHudFromState` 再 `continue`；
**第一次真正渲染之后**把 `matchResumeSimT = null`（避免影响下一批调用）。
**不需要改 `js/match.js` 的签名。**

**完成判据（三条都要）**：

1. 用 `scripts/_live-visual-audit.mjs`（已能驱动真实直播）断言
   **续播后首帧 `simT ≈ 续播点`**，且**比分/事件数与不续播时一致**；
2. **跨半场必须实测**：H2 续播时 H1 整段跳过 ⇒ 会先停在**中场面板**
   （这未必是坏事：用户可顺手调整），但要么在文案里说清、要么显式跳过；
3. **`applyHalfTimeSwap` 回归**：定义 `js/match.js:1472`，只在 `fromMin === 46` 触发
   （`:1510`）。整段跳过**不应**影响它 —— 但 **v279 刚修完换边，必须实测**：
   跑 `node scripts/ends-swap-verify.mjs`（应仍 33/33），并另跑一场下半场直播看端别。

### 第二优先：两个待拍板的语义问题（都要用户先定方向）

| 问题 | 现状 | 为什么没改 |
|---|---|---|
| **「反击」风格没有「防守深」那一半** | 引擎里 counter 只有「转换期猛冲」（`attackDepthShift +4`），`_defLineY` 不读 style | 试了两版：v1 让穿透球**反降**（0.069→0.028，方向错——`depthShift` 作用于**持球期**，全队压深后跑动者越不过越位线）；v2 只有 **0.43σ**，且**推翻** `scripts/team-shapes-audit.mjs:75` 的既有钉定断言。撤回理由写在 `js/team-shapes.js:132` 附近 |
| **`vision` 提不高穿透球占比** | 直塞要过 **5 道硬门槛**才掷骰（`engine.js:3515-3523`），且直塞选项被 `value *= 0.72` 惩罚 | 改门槛会牵动 AGENTS.md 的「**直塞第 2 门**」标定 —— **⛔ 动它之前先读那一节** |

### 第三优先：从未量过的空档

- **`roles`/`duties` 的进攻端影响**：现有 `scripts/player-roles-audit.mjs` 只覆盖**防守职责分配**
  （`cm_ballwinner` 抢压迫权、高压 duty 站得更近）。**进攻端跑位从没量过。**
- **`decisions` 需要新判据**：它只改 softmax 温度 `0.38 → 0.302`（**1.26×**，`engine.js:3195`），
  而同一行的 `core`/`isWing` 把基准从 0.35 挪到 0.28/0.30 —— **角色项强得多**。
  三个代理量（穿透/传中占比、被断率）都测不出。要直接测「被选选项的权重分布」。

### 第四优先：本会话**量过但没处理**的

来自 `scripts/_live-visual-audit.mjs` 跑真实直播的读数（**用户当时把话题转向了球员行为**，所以留着没动）：

| # | 读数 | 含义 |
|---|---|---|
| P1 | canvas 缓冲区恒 **1026×665**，而显示尺寸 **1218~1375 × 789~891** | 每个显示像素只有 0.75~0.84 个缓冲像素 ⇒ 被放大 **1.19~1.34×** ⇒ **糊** |
| P2 | 显示宽在 **1314 / 1334 / 1375** 三态间来回（直方图三峰 51/48/45） | 整块画面在周期性缩放；dock 高度恒定 ⇒ 不是文字挤的 |
| P4 | rAF 间隔最大 **5131 ms**（两次跑都发生在开播后 ~5.6s） | 一次约 5 秒冻住；**疑似截图副作用，未定** |

⚠ 采集产物在 `.tmp-watch/`（**已 gitignore，换机后不在了**）。要重现就重跑探针。

### 第五优先：两个孤立的待查项

- **`js/matchview-coords.js` 的 `endsSwapped` 没有接线**：v279 已给它加好 `setEndsSwapped()`，
  但**生产代码里没人调**。接线只需一行（`_applyEndsChrome`），
  但会同时暴露 `_goalTarget` 的两处 H1 写死（`js/matchview.js:4784` 的 `±3` nudge、
  `:4859` 的 `attHome ? 2 : 10`）—— **要同批处理**。
- **`createMatchSession` 可能覆盖显式首发**：v278 的探针显示**建会话之后门将槽站的是一名后卫**
  （`h_d0(DEF)`），而队里有 2 名真门将且已显式排进首发。对体能那条线无影响
  （按 id 读 `runMetres`），但对**现实性**可能有影响。**另行排查，不要并进体能那条线。**

### 已知未结案的测量缺陷

- **`scripts/_fitness-finalize-drain-probe.mjs` 当前不可逐位复现**：同一份代码同一份输入连跑两次，
  球员跑动距离与换人次数都不同。已排除 `Math.random`（进程内已替换）、`fixture.matchSeed`（已显式）、
  属性生成、时钟；**根因未定位**。⇒ 引用它的跨轮数字**只能当量级参考**，
  受控证据是**同一轮内**的配对（见 `finalize-fitness-distance-share-2026-09-21.txt` §9）。

## 6. 本会话踩过、换机后别再踩的坑

### 方法论（三条，都是本会话反复证明的）

1. 🔴 **判据要量「旋钮 / 缺陷代码里直接改的那个量」** —— 本会话**栽了三次**：
   - v279：用「总进球数 ≤ 12」「射门纵深」这类**总量**指标，**漏掉了「进球记给谁」**
     （8 球 8 反而指标全绿）；
   - v280：用「传球数」判 `tempo` 得出「tempo 是死的」——**错**；它改的是「拿球后多久决定」，
     换成**持球时长**后立刻 6σ；
   - v280 快档：用行为量判 `defensiveLine`/`pressing`，信号被稀释成 3.4 / 1.2 格；
     改成**直接调 `_defLineY()`**（受控局面）后零噪声、−15.2 / −11.2 格。
   ⇒ **问「总量像不像」会漏掉「具体事实对不对」；问「间接指标动不动」会漏掉「直接量变没变」。**
2. 🔴 **静态断言 ≠ 功能可用**：本仓库血例是进场动画（单测 17/17，画面上**什么都没发生**）。
   v281 因此加了浏览器层（`npm run test:reopen-browser`）。
   **凡「必须让用户看见」的东西，都要用真实 Chromium 验一次。**
3. 🔴 **`indexOf(名字)` 命中的是「调用点」不是「定义」**：v277 的
   `segment-cut-fade-verify.mjs` 因此假红 7 项（而产品代码是对的）。
   正确做法：锚「**行首缩进 + 签名 + `{`**」+ **体长落在预期区间**双保险。

### 纪律

4. **撤回纪律**：证据不足（0.43σ）**且**会推翻既有钉定断言 ⇒ **撤回**，
   **不要改断言去迁就自己的改动**（v280「反击」就是这么处理的）。
5. **改预缓存资源必须升 `sw.js` 的 `CACHE`**：版本号 **12 处**（含 `index.html` 的
   `vcfm-sw-reloaded-vN`），`node scripts/cache-audit.mjs` 会核对
   —— 它还会断言 **AGENTS.md 页首**记的是当前缓存，忘了改 AGENTS.md 会红。
6. **改默认档要过「逐位等价」**：换边改动必须 `endsSwapped=false` 逐位相同
   （做法：`git show HEAD` 取改前基线，同输入逐帧对比）。**默认档改动也要过这一关** ——
   v280 的 `pressPush` 写成 `(pressLevel - 3)` 就是为了这个。
7. **多行编辑前必须先 `Read` 现行文件**（本会话在写这份交接时又犯了一次：
   用想象的行号做多点编辑，把文档弄乱，只能整份重写）。

### 工具链

8. **PowerShell 的 `>` 会写成 UTF-16**，后续读不了（报 "binary content"）。
   长任务一律 `cmd /c "node 脚本 > 日志 2>&1"`。
9. **Git 推送必须显式补回凭据助手**：
   `& "C:\Program Files\Git\cmd\git.exe" -c credential.helper=manager push origin master`。
10. **不要在 PowerShell 里用 `|` 拼多个 `Select-String` 模式**（会被当成位置参数报错）—— 逐条写。

## 7. 可复现命令（含实测耗时）

```bash
# —— 快（秒级）——
node scripts/ends-swap-verify.mjs              # 换边方向/端别 33/33（约 2 秒）
node scripts/tactics-response-verify.mjs       # 四个旋钮的响应（9 秒）
node scripts/reopened-match-notice-audit.mjs   # 重开提示：12 静态 + 7 变异（瞬时）
node scripts/segment-cut-fade-verify.mjs       # 切段淡场三处（1 秒）
node scripts/cache-audit.mjs                   # 版本号 12 处 + AGENTS.md

# —— 中（分钟级）——
node scripts/fitness-finalize-drain-verify.mjs         # 终场体能分摊（30 秒）
node scripts/_tactics-understanding-probe.mjs 4 30     # 战术层摸底（260 秒）

# —— 浏览器（需 Playwright + msedge）——
npm run test:reopen-browser                    # 重开提示真可见（146 秒）
node scripts/_live-visual-audit.mjs 90 live    # 直播取证：三层读数 + 截图（约 220 秒）
node scripts/_segment-boundary-displacement.mjs 75 live   # 切段位移 + fadeOpacity（约 245 秒）

# —— 全量 ——
node scripts/verify.mjs                        # 114 项（43~57 分钟）
```

本机环境（本次全程使用）：**node v24.17.0**（系统）、**python 3.13.14**
（探针起 `http.server` 要用）、Playwright + msedge 已装。
上一份交接提到的托管 Node
（`C:/Users/WXX/.workbuddy-ai/binaries/node/versions/22.22.2-2/node.exe`）本次未使用。

## 8. 本会话入库的新文件

| 文件 | 用途 |
|---|---|
| `scripts/ends-swap-verify.mjs` | **换边方向/端别回归 33/33**（已挂 `verify.mjs`，2 秒） |
| `scripts/tactics-response-verify.mjs` | 四个战术旋钮的响应（已挂 `verify.mjs`，9 秒） |
| `scripts/reopened-match-notice-audit.mjs` | 重开提示的静态 + 变异测试（已挂，瞬时） |
| `scripts/reopened-match-notice-browser-check.mjs` | 重开提示的**浏览器层**（`npm run test:reopen-browser`，146 秒） |
| `scripts/fitness-finalize-drain-verify.mjs` | 终场体能分摊（已挂，30 秒） |
| `scripts/segment-cut-fade-verify.mjs` | 切段淡场三处（已挂，1 秒） |
| `scripts/_tactics-understanding-probe.mjs` | 战术层/个体层**摸底探针**（配对 A/B，260 秒） |
| `scripts/_fitness-finalize-drain-probe.mjs` | 终场体能取证（⚠ 当前不可逐位复现，见 §5） |
| `scripts/_live-visual-audit.mjs` | **直播取证**：canvas / 实体 / DOM 三层 + 截图 |
| `scripts/_watch-contact-sheet.mjs` | 把截图拼成可翻阅页面（`.tmp-watch/index.html`） |
| `docs/measurements/tactics-layer-four-items-2026-09-22.txt` | **本会话最全的归档**（含 ④-B 配方附录） |
| `docs/measurements/tactics-understanding-baseline-2026-09-22.txt` | 战术层摸底读数 |
| `docs/measurements/finalize-fitness-distance-share-2026-09-21.txt` | 终场体能（含 §9 未复现说明） |
| `docs/measurements/segment-cut-fade-fix-verify-2026-09-21.txt` | 切段淡场修后复测 + 变异测试 |

## 9. 别重开 / 别再做（累积）

- **形状压缩**（`CB_BLOCK_SHIFT_*` tail）：09-16 已走到头，再加压会擦强弱分离下限。
- **「直塞第 2 门」的 0.20**：⛔ 动它之前先读 `AGENTS.md` 那一节（0.15 / 0.25 两边都试过）。
- **「读引擎 `agent.fitness` 回写」**修中场体能：实测修不好（个体 spread 只有 0.39）。
- **给段入口加缓动**：已证伪（真实间隔 129.5~956.8 比赛秒，0.7 s 平移 45 m = 64 m/s 扫掠）。
- **改 DOM `.mp-player` 的 `left/top`**：场上 22 人是 **canvas** 画的。
- **跑 `match-motion-integrity-audit.mjs` 来「证明」瞬移好了**：它喂 `record(snapshot, snapshot)`，
  画面层结构上永不可达。
- **「反击」的 regroup 改动**：没有新证据就别重试（见 §5 第二优先）。
